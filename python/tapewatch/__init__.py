"""Shared Tapewatch Python library: config loading, the tape format, the
alert/case store, and SVG rendering for the report.

Nothing here depends on anything outside the standard library.
"""

__all__ = ["miniyaml", "model", "tape", "store", "svg", "config"]
